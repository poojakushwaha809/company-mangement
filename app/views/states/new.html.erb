<%= form_for @state do |f| %>
 
  <% if @state.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@state.errors.count, "error") %> prohibited
        this state from being saved:
      </h2>
      <ul>
        <% @state.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    

 <p>
    <%= f.label :select_country %><br>
    <%= f.collection_select :country_id, Country.order(:country_name),:id,:country_name, include_blank: true %>
  </p>

  <p>
    <%= f.label :state_name %><br>
    <%= f.text_field :state %>
  </p>
 
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %>